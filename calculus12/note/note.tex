\documentclass[a4paper, leqno]{article}

% --- Encoding / Fonts ---
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}

% --- Math ---
\usepackage{amsmath, amssymb, amsthm}
\numberwithin{equation}{subsection}

% --- Page style / Graphics ---
\usepackage{fancyhdr}
\usepackage{graphicx}
\usepackage{xcolor}

% --- Lists ---
\usepackage{enumitem}

% --- TikZ / PGFPlots ---
\usepackage{tikz}
\usetikzlibrary{
  calc,
  angles,
  quotes,
  intersections,
  arrows.meta,
  positioning
}

\usepackage{pgfplots}
\pgfplotsset{
  width=10cm,
  compat=1.9
}

\definecolor{ink}{HTML}{111111}
\definecolor{soft}{HTML}{666666}
\definecolor{accent}{HTML}{B23A48}

\tikzset{
  line/.style={draw=ink, line width=.55pt},
  ray/.style={draw=ink, line width=.55pt, -{Stealth[length=1.7mm]}},
  aux/.style={draw=soft, line width=.4pt, dashed},
  anglemark/.style={draw=accent, line width=.55pt},
  lab/.style={font=\scriptsize, inner sep=1pt},
  tiny lab/.style={font=\scriptsize, inner sep=0.8pt}
}

\newcommand{\miniTitle}[1]{\vspace{.6em}\noindent\textbf{#1}\par\vspace{.15em}}

% --- Multi-column layout, useful for compact note sheets ---
\usepackage{multicol}

% --- Optional layout packages ---
% \usepackage{geometry}
% \usepackage{indentfirst}

% --- Index ---
\usepackage{imakeidx}
\makeindex[intoc, title=Index]

% --- Hyperref: keep near the end ---
\usepackage[hidelinks]{hyperref}

\definecolor{sepiabg}{RGB}{230, 235, 220}
\definecolor{sepiabg}{RGB}{244,236,220} % 柔和的淡米色
\pagecolor{sepiabg}

% --- Page Setup ---
\pagestyle{fancy}
\fancyhf{}
\lhead{\emph{\nouppercase{\leftmark}}}
\rhead{%
  \ifnum\value{page}=1
  \else
    Calculus and Vector
  \fi
}
\cfoot{\thepage}
\setlength{\headheight}{14pt}

% --- Theorem Environments ---
\theoremstyle{plain}
\newtheorem{theorem}{Theorem}[subsection]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{example}[theorem]{Example}

\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}

\theoremstyle{remark}
\newtheorem*{remark}{Remark}

% --- Semantic (unnumbered) environments ---
\theoremstyle{definition}
\newtheorem*{axiom}{Axiom}
\newtheorem*{assumption}{Assumption}
\newtheorem*{notation}{Notation}

% --- Index term command ---
\newcommand{\term}[1]{\emph{#1}\index{#1}}

% --- Lists ---
\setlist[itemize]{label=--}
\setlist[itemize,2]{label=$\circ$}

% --- QED symbol ---
\renewcommand{\qedsymbol}{$\square$}

% --- Custom Commands ---
\newcommand{\R}{\mathbb{R}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\Q}{\mathbb{Q}}
\newcommand{\C}{\mathbb{C}}
\newcommand{\ds}{\displaystyle}
\newcommand{\blue}[1]{\textcolor{blue}{#1}}
\newcommand{\red}[1]{\textcolor{red}{#1}}

\newcommand{\mypic}[3]{%
  \begin{figure}[h!]
    \centering
    \includegraphics[width=#3\textwidth]{#1}
    \caption{#2}
  \end{figure}
}

\newenvironment{worddef}[1]
  {\par\noindent\textbf{#1:} \itshape}
  {\par\normalfont}

% --- Title ---
\title{\textbf{OSSD - Calculus and Vector} \\ \large MCV4U}
\author{Qinghao Hu}
\date{\today}

\includeonly{chapters/chapter7/c7}

\begin{document}

\maketitle
{\small
\input{introduction.tex}
}
\newpage
\tableofcontents
\newpage

\include{chapters/chapter1/c1}
\include{chapters/chapter2/c2}
\include{chapters/chapter3/c3}
\include{chapters/chapter4/c4}
\include{chapters/chapter5/c5}
\include{chapters/chapter6/c6}
\include{chapters/chapter7/c7}
% \include{chapters/referenceSheet/reference}


\end{document}

% 你是一名精通 LaTeX/TikZ 的数学笔记绘图助手。

% 请根据我给出的数学主题或图形描述，生成可以直接插入到以下 LaTeX 文档中的 TikZ 代码。我的主文档已经加载了这些包和设置：

% - tikz
% - pgfplots
% - TikZ libraries: calc, angles, quotes, intersections, arrows.meta, positioning
% - 已定义颜色：ink, soft, accent
% - 已定义 TikZ styles：
%   line/.style={draw=ink, line width=.55pt}
%   ray/.style={draw=ink, line width=.55pt, -{Stealth[length=1.7mm]}}
%   aux/.style={draw=soft, line width=.4pt, dashed}
%   anglemark/.style={draw=accent, line width=.55pt}
%   lab/.style={font=\scriptsize, inner sep=1pt}
%   tiny lab/.style={font=\scriptsize, inner sep=0.8pt}

% 生成要求：

% 1. 只输出可以直接插入正文的 LaTeX 代码，不要输出完整导言区，不要使用 \documentclass。
% 2. 图形必须使用 TikZ，格式如下：

% \[
% \begin{tikzpicture}[scale=..., >=Stealth]
%   ...
% \end{tikzpicture}
% \]

% 或者如果更适合居中展示，可以使用：

% \begin{center}
% \begin{tikzpicture}[scale=..., >=Stealth]
%   ...
% \end{tikzpicture}
% \end{center}

% 3. 必须优先使用我已经定义好的样式：
%    - 普通线段用 [line]
%    - 射线或向量箭头用 [ray]
%    - 辅助线用 [aux]
%    - 角标记用 [anglemark]
%    - 标签用 [lab] 或 [tiny lab]

% 4. 不要使用未定义的 TikZ style，例如不要使用 mark、my angle、vector 等未定义样式。
% 5. 如果需要标记角度，必须使用已加载的 angles, quotes 库，并用类似下面的安全写法：

% \pic [anglemark, "$\theta$", angle radius=7mm, angle eccentricity=1.25]
%   {angle = A--O--B};

% 6. 如果画向量，推荐写法：

% \draw[ray] (0,0) -- (2,1) node[midway, above, lab] {$\vec{v}$};

% 7. 图形风格要简洁、适合高中 Calculus and Vectors 笔记：
%    - 不要过度装饰
%    - 标签清晰
%    - 线条细
%    - 尺寸紧凑
%    - 尽量避免复杂颜色，只使用 ink, soft, accent

% 8. 代码必须能在我的模板下直接编译，不要引入新 package。
% 9. 如果图形有数学定义、公式或简短说明，可以在图下方加一句简短说明，但不要写太长。
% 10. 输出前请自行检查 TikZ 语法，避免以下常见错误：
%     - /tikz/mark requires a value
%     - Undefined control sequence
%     - Missing } inserted
%     - You can't use macro parameter character # in horizontal mode
%     - angle 语法错误
%     - 使用了未加载的 TikZ library